{
  "name": "hustlerteam",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "esbuild server/index.ts --platform=node --bundle --format=esm --external:lightningcss --external:vite --outdir=dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.21.2"
  }
}
